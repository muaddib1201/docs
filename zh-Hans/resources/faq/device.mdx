---
title: 设备管理问题
description: 设备状态查询与状态一致性相关常见问题。
---

# 设备管理问题

### 为什么 SDK 返回的状态和打印机面板显示的状态不一致？

SDK 返回的状态取决于打印机固件上报的状态。部分特殊情况下，打印机面板显示了故障状态，但 SDK 未返回对应状态。例如 `e_model_series_4020` 系列，打印机面板显示"缺纸纸盒 选配 1-2"，但 SDK 返回的状态为"打印模块空闲"——这是打印机固件设计决定的，此种情况不会上报对应状态。
